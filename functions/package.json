{
  "name": "nuts-cloud-functions",
  "description": "Firebase Cloud Functions for NUTS Sports Pick'em Platform",
  "version": "1.0.0",
  "main": "index.js",  "scripts": {
    "build": "echo 'No build step needed - JavaScript functions'",
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "firebase-admin": "^11.11.0",
    "firebase-functions": "^4.5.0",
    "cors": "^2.8.5",
    "crypto": "^1.0.1",
    "xrpl": "^2.12.0"
  },
  "devDependencies": {
    "firebase-functions-test": "^3.1.0"
  },
  "private": true
}
