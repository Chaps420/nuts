import 'package:flutter_web_plugins/flutter_web_plugins.dart';

/// Web implementation of XamanFirebaseAuth
class XamanFirebaseAuthWeb {
  /// Registers this class as the default instance of [XamanFirebaseAuthWeb].
  static void registerWith(Registrar registrar) {
    // This is required for web platform but the actual implementation
    // is in the main library since it uses the same code for all platforms
  }
}